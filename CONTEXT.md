# 用語

**Nyaa Enhancer Presets**: `Arad119/Nyaa-Enhancer` を基にした Chrome 向けフォークの名称。GitHub リポジトリは `came815/nyaa-enhancer-presets` を予定する。

**期間プリセット**: 検索開始時点を基準にした相対アップロード期間。Day、Week、Month、3Month、Year を指し、Month は 30 日間の既定値である。

**検索開始時刻**: 検索を開始した瞬間に固定する時刻。期間の判定は追加読み込みの途中でもこの時刻を基準にする。

**Show more**: 現在の検索条件で次の結果を増分取得して既存結果の末尾へ追加する操作。全期間の結果が取得できることを意味しない。

**進行位置**: 追加読み込みを再開するために必要な取得済み範囲の状態。検索条件とともに URL 条件として保存する予定である。

**Chrome 用リリース ZIP**: Chrome の「パッケージ化されていない拡張機能を読み込む」で選ぶフォルダー直下に `manifest.json` がある配布物。対応するソースとライセンス・帰属情報を併せて提供する予定である。
