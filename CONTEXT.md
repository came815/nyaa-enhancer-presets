# 用語

**Nyaa Enhancer Presets**: `Arad119/Nyaa-Enhancer` を基にした Chrome 向けフォークの名称。GitHub リポジトリ `came815/nyaa-enhancer-presets` で公開している。

**期間プリセット**: 検索開始時点を基準にした相対アップロード期間。Day、Week、Month、3Month、Year を指し、Month は 30 日間の既定値である。

**検索開始時刻**: 検索を開始した瞬間に固定する時刻。期間の判定は追加読み込みの途中でもこの時刻を基準にする。

**Show more**: 現在の検索条件で次の結果を増分取得して既存結果の末尾へ追加する操作。全期間の結果が取得できることを意味しない。

**自動読み込み**: 結果の末尾から約600px以内に近づくと、Show moreと同じ取得処理を自動で開始する機能。初期値はオンで、ユーザーのオン・オフ選択は拡張設定に保存する。

**一時停止**: 自動取得を止め、明示的な再開を待つ状態。ユーザー操作のほか、通信エラー・アクセス制限・タイムアウト・10ページ連続で表示対象がない場合に停止する。非表示タブでは新しい取得を始めず、表示復帰時は他の停止理由がなければ自動取得を再開できる。

**進行位置**: 追加読み込みを再開するために必要な取得済み範囲の状態。ページ内で保持し、再読み込み時はURLに示す開始ページから表示し直す。URLには検索条件を保持するが、追加取得した行と進行位置は保存しない。

**Chrome 用リリース ZIP**: Chrome の「パッケージ化されていない拡張機能を読み込む」で選ぶフォルダー直下に `manifest.json` がある配布物。対応するソースとライセンス・帰属情報を同じリリースで提供する。

**表示言語**: 拡張が追加する画面やメッセージの言語。日本語が初期値で、Englishに切り替えられる。選択は拡張設定に保存し、切り替えた画面を再読み込みして反映する。他のタブには次回の再読み込み時に反映する。サイトの作品名・本文・ユーザー入力・外部APIの作品情報は翻訳しない。
